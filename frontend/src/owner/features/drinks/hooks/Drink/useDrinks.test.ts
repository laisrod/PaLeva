import { describe, it, expect } from 'vitest'

describe('frontend/src/owner/features/drinks/hooks/Drink/useDrinks.ts', () => {
  it('has a placeholder test', () => {
    expect(true).toBe(true)
  })
})
