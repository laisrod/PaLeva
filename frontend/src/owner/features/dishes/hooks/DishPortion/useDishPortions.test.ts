import { describe, it, expect } from 'vitest'

describe('frontend/src/owner/features/dishes/hooks/DishPortion/useDishPortions.ts', () => {
  it('has a placeholder test', () => {
    expect(true).toBe(true)
  })
})
