import { describe, it, expect } from 'vitest'

describe('frontend/src/owner/features/dishes/hooks/Dish/useDeleteDish.ts', () => {
  it('has a placeholder test', () => {
    expect(true).toBe(true)
  })
})
