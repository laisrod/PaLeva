import { describe, it, expect } from 'vitest'

describe('frontend/src/owner/features/menus/hooks/Menu/useEditMenu.ts', () => {
  it('has a placeholder test', () => {
    expect(true).toBe(true)
  })
})
